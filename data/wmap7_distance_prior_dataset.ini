DataName = WMP7_Distance_Prior

# if format = 0, then use WMAP (lA, R, Z_rec) as the observables
# if format = 1, then use Planck (R, lA, Omegabh2) as the observables
format = 0

use_Hu_fitting = T


dist_prior_file = data/CMB_dist_prior/wmap7_dist_prior.txt
inv_covmat_file = data/CMB_dist_prior/wmap7_dist_prior_inv_covmat.txt

